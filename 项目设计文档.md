# task228-seedgerm replacement 项目设计文档

## 目标用户与闭环

实验人员导入单粒种子的图像时序与试验环境采样，服务检测萌发生理阶段，人工复核冲突或停滞证据，并发布可替代的试验结果版本。核心闭环是：创建试验 → 录入种子/图像/环境 → 检测与确认阶段 → 复核 → 结果版本发布 → 重启后继续读取证据。

## 实体与状态

试验为 planned、running、reviewing、completed、sealed；种子为 pending、observing、germinated、stalled、contaminated；阶段事件为 candidate、confirmed、conflict、revoked；结果版本为 draft、pending、published、superseded。

## 持久化与恢复

SQLite WAL 持久化 trials、seeds、seed_images、env_samples、stage_events、observations、trial_results。图像自然键为 seed_id+hash，环境自然键为 trial_id+sampled_at+instrument，结果按 trial_id+version 唯一。服务重启后从数据库恢复全部状态。

## 入口与模块责任

HTTP `/api` 暴露试验、种子、图像、环境、阶段、复核、结果和自检入口；`service` 做生命周期及实体边界编排；`ingest` 管理图像时间/幂等；`enviro` 管理环境窗口；`stage` 管理阶段检测与确认；`review` 管理人工复核；`result` 管理版本；`store` 负责 SQLite 约束和事务。

## 并发与错误边界

同试验结果版本分配必须串行化；冲突解决必须原子提交；封存试验拒绝子资源变更；污染证据不可逆；同一实体关系的跨 ID 操作必须拒绝；时间线不得回退；自然键重试不能制造重复记录。

## 端到端验收场景

覆盖图像重试、环境毫秒时间、封存阶段、污染不可逆、阶段时间线、跨种子复核、冲突事务、并发结果版本、结果替代链和自检证据分桶。每道题使用独立用户可见行为 probe，并以同一命令完成 red→green。
